// importation de la class Student
import { Student } from "./Student.js";

// création d'une instance de la class Student avec des parametres spécifiques
let student = new Student("Momo", 24, "ESP");

// appel de la method greet sur l'instance student
student.greet()